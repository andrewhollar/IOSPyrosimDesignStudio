

pushing changes:

1. git add -A
2. git commit -m "message"
3. git push origin master   <--- or replace master with branch name


Force Reset:

1. git fetch origin
2. git reset --hard origin/master


Branches:

git checkout -b <new-branch>
git branch -d branch_name
git checkout <branch-name>
